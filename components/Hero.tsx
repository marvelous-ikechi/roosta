import Link from 'next/link';

function Hero() {
  return (
    // ... (rest of your Hero content)
    <section className="bg-white pt-24 pb-24 text-center">
        {/* ... */}
    </section>
  );
}

// Add: Export the component as the default
export default Hero;